id: neg-acr-build
name: Reject — ACR Build
description: Azure Container Registry image builds are out of scope. Redirect to azure-deploy or `az acr
  build` docs.
tags:
- negative
- routing
- acr
inputs:
  prompt: Build and push my Docker image to ACR
expected:
  should_trigger: false
graders:
- type: prompt
  name: behavior-neg-acr-build
  config:
    prompt: 'The user asked: "Build and push my Docker image to ACR"


      Azure Container Registry image builds are out of scope. Redirect to azure-deploy or `az acr build`
      docs.


      This is OUT OF SCOPE for aca-sandboxes. The skill should either not trigger, or briefly redirect
      to the correct skill/product. It must NOT provide a full walkthrough for this query.


      If the response correctly avoids deep engagement, call set_waza_grade_pass.


      If it provides a detailed walkthrough for this out-of-scope query, call set_waza_grade_fail.'
    continue_session: true
