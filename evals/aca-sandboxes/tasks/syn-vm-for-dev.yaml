id: syn-vm-for-dev
name: Synthetic — VM For Dev Work
description: 'Ambiguous: "VM for dev work" matches ACA Sandbox, Azure VM, Codespaces, GitHub Codespaces,
  Dev Box. Skill should activate (ACA Sandboxes ARE dev VMs) but disambiguate by asking about lifecycle,
  MCP needs, agent integration. If user just wants a long-lived workstation VM with no agent/MCP context,
  redirect to Microsoft Dev Box.'
tags:
- synthetic
- ambiguous
- dev-environment
inputs:
  prompt: I want a VM I can SSH into for dev work in Azure
expected:
  should_trigger: true
graders:
- type: prompt
  name: behavior-syn-vm-for-dev
  config:
    prompt: 'The user asked: "I want a VM I can SSH into for dev work in Azure"


      Ambiguous: "VM for dev work" matches ACA Sandbox, Azure VM, Codespaces, GitHub Codespaces, Dev Box.
      Skill should activate (ACA Sandboxes ARE dev VMs) but disambiguate by asking about lifecycle, MCP
      needs, agent integration. If user just wants a long-lived workstation VM with no agent/MCP context,
      redirect to Microsoft Dev Box.


      Note on ambiguity: Dev VM is overloaded — Dev Box vs Codespaces vs ACA Sandbox vs Azure VM.


      If the response follows the expected behavior described above, call set_waza_grade_pass.


      If it fails to meet the expected behavior, call set_waza_grade_fail.'
    continue_session: true
