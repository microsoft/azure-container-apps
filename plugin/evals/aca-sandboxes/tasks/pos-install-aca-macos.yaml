id: pos-install-aca-macos
name: Install aca CLI on macOS
description: Skill activates. Provides the macOS install one-liner from public docs (`curl -fsSL ... |
  bash` against the public release host), then `aca doctor` to verify 8/8 checks pass, then `aca login`.
  Notes minimum version 1.0.0-beta.1.
tags:
- positive
- install
- macos
- cli
inputs:
  prompt: How do I install the aca CLI on my Mac?
expected:
  should_trigger: true
graders:
- type: prompt
  name: behavior-pos-install-aca-macos
  config:
    prompt: 'The user asked: "How do I install the aca CLI on my Mac?"


      Skill activates. Provides the macOS install one-liner from public docs (`curl -fsSL ... | bash`
      against the public release host), then `aca doctor` to verify 8/8 checks pass, then `aca login`.
      Notes minimum version 1.0.0-beta.1.


      If the response follows the expected behavior described above, call set_waza_grade_pass.


      If it fails to meet the expected behavior, call set_waza_grade_fail.'
    continue_session: true
