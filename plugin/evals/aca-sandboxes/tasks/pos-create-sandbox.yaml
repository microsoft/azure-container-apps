id: pos-create-sandbox
name: Create First Sandbox
description: 'Skill activates. Walks user through: (1) `aca sandboxgroup create --location westus3 --name
  <grp> --set-config`, (2) `aca sandbox create --group <grp> --id <name> --disk ubuntu-24.04`, (3) `aca
  sandbox shell -l name=<name>`. Uses `--location` NOT `--region`. Mentions auto-suspend gotcha.'
tags:
- positive
- create
- quickstart
- cli
inputs:
  prompt: Create my first ACA Sandbox
expected:
  should_trigger: true
graders:
- type: prompt
  name: behavior-pos-create-sandbox
  config:
    prompt: 'The user asked: "Create my first ACA Sandbox"


      Skill activates. Walks user through: (1) `aca sandboxgroup create --location westus3 --name <grp>
      --set-config`, (2) `aca sandbox create --group <grp> --id <name> --disk ubuntu-24.04`, (3) `aca
      sandbox shell -l name=<name>`. Uses `--location` NOT `--region`. Mentions auto-suspend gotcha.


      If the response follows the expected behavior described above, call set_waza_grade_pass.


      If it fails to meet the expected behavior, call set_waza_grade_fail.'
    continue_session: true
