id: neg-app-service-deploy
name: Reject — App Service Deploy
description: App Service deployments are out of scope. Redirect to azure-deploy.
tags:
- negative
- routing
- app-service
inputs:
  prompt: Deploy my Node app to Azure App Service
expected:
  should_trigger: false
graders:
- type: prompt
  name: behavior-neg-app-service-deploy
  config:
    prompt: 'The user asked: "Deploy my Node app to Azure App Service"


      App Service deployments are out of scope. Redirect to azure-deploy.


      This is OUT OF SCOPE for aca-sandboxes. The skill should either not trigger, or briefly redirect
      to the correct skill/product. It must NOT provide a full walkthrough for this query.


      If the response correctly avoids deep engagement, call set_waza_grade_pass.


      If it provides a detailed walkthrough for this out-of-scope query, call set_waza_grade_fail.'
    continue_session: true
