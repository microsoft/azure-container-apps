id: syn-ai-agent-runtime
name: Synthetic — AI Agent Runtime
description: 'Ambiguous: AI agents can run on Foundry, Container Apps, ACA Sandboxes (for personal/dev),
  Dynamic Sessions (for tool code execution), Functions, etc. Skill should activate (Personal Agent is
  a flagship template) but ask whether the user wants: (a) a personal dev sandbox for THEIR agent, (b)
  a managed runtime for END USERS'' agent code execution.'
tags:
- synthetic
- ambiguous
- ai-agent
inputs:
  prompt: What should I use to run my AI agent?
expected:
  should_trigger: true
graders:
- type: prompt
  name: behavior-syn-ai-agent-runtime
  config:
    prompt: 'The user asked: "What should I use to run my AI agent?"


      Ambiguous: AI agents can run on Foundry, Container Apps, ACA Sandboxes (for personal/dev), Dynamic
      Sessions (for tool code execution), Functions, etc. Skill should activate (Personal Agent is a flagship
      template) but ask whether the user wants: (a) a personal dev sandbox for THEIR agent, (b) a managed
      runtime for END USERS'' agent code execution.


      Note on ambiguity: Multiple Azure agent runtimes exist — disambiguate developer-owned vs. managed
      multi-tenant.


      If the response follows the expected behavior described above, call set_waza_grade_pass.


      If it fails to meet the expected behavior, call set_waza_grade_fail.'
    continue_session: true
