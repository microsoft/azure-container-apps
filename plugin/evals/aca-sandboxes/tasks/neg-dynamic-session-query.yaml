id: neg-dynamic-session-query
name: Reject — Dynamic Sessions (Disambiguation)
description: 'CRITICAL: this is the Container Apps Dynamic Sessions product — a DIFFERENT product from
  ACA Sandboxes. The skill must NOT activate to run untrusted code in a sandbox. It should explicitly
  call out the disambiguation: Dynamic Sessions = managed code-interpreter for AI agents to run untrusted
  code in seconds-long ephemeral Hyper-V isolated sessions; ACA Sandboxes = developer-owned long-lived
  microVMs with direct programmable control. Redirect to Container Apps Dynamic Sessions docs.'
tags:
- negative
- routing
- dynamic-sessions
- disambiguation
- critical
inputs:
  prompt: I need a sandbox to run untrusted Python code generated by my LLM agent — what should I use?
expected:
  should_trigger: false
graders:
- type: prompt
  name: behavior-neg-dynamic-session-query
  config:
    prompt: 'The user asked: "I need a sandbox to run untrusted Python code generated by my LLM agent
      — what should I use?"


      CRITICAL: this is the Container Apps Dynamic Sessions product — a DIFFERENT product from ACA Sandboxes.
      The skill must NOT activate to run untrusted code in a sandbox. It should explicitly call out the
      disambiguation: Dynamic Sessions = managed code-interpreter for AI agents to run untrusted code
      in seconds-long ephemeral Hyper-V isolated sessions; ACA Sandboxes = developer-owned long-lived
      microVMs with direct programmable control. Redirect to Container Apps Dynamic Sessions docs.


      This is OUT OF SCOPE for aca-sandboxes. The skill should either not trigger, or briefly redirect
      to the correct skill/product. It must NOT provide a full walkthrough for this query.


      If the response correctly avoids deep engagement, call set_waza_grade_pass.


      If it provides a detailed walkthrough for this out-of-scope query, call set_waza_grade_fail.'
    continue_session: true
