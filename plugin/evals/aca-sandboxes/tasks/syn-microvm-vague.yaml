id: syn-microvm-vague
name: Synthetic — Vague microVM
description: 'Ambiguous: `microvm` could mean ACA Sandboxes, Firecracker generally, or Azure VM. Skill
  should activate since ACA Sandboxes ARE Azure-hosted microVMs, but should ask whether the user wants
  a dev-loop microVM (ACA Sandbox) vs. a production VM (Azure VM).'
tags:
- synthetic
- ambiguous
- microvm
inputs:
  prompt: I need a microVM in Azure
expected:
  should_trigger: true
graders:
- type: prompt
  name: behavior-syn-microvm-vague
  config:
    prompt: 'The user asked: "I need a microVM in Azure"


      Ambiguous: `microvm` could mean ACA Sandboxes, Firecracker generally, or Azure VM. Skill should
      activate since ACA Sandboxes ARE Azure-hosted microVMs, but should ask whether the user wants a
      dev-loop microVM (ACA Sandbox) vs. a production VM (Azure VM).


      Note on ambiguity: "microvm" is generic; ACA Sandboxes are microVMs but Azure VMs and AKS Kata Containers
      also fit.


      If the response follows the expected behavior described above, call set_waza_grade_pass.


      If it fails to meet the expected behavior, call set_waza_grade_fail.'
    continue_session: true
