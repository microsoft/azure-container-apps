id: compare-sandbox-vs-container-app
name: Compare — Sandbox vs Container App
description: 'Compare-style query. Skill activates. Explains: ACA Sandbox = single microVM you own + SSH
  into for dev loop, MCP hosting, agent dev. Container App = the existing serverless container service
  (auto-scale 0→N, HTTPS ingress, revisions). Different lifecycle, different audience. References public
  sandboxes-overview.md.'
tags:
- compare
- container-apps
inputs:
  prompt: How is an ACA Sandbox different from a regular Container App?
expected:
  should_trigger: true
graders:
- type: prompt
  name: behavior-compare-sandbox-vs-container-app
  config:
    prompt: 'The user asked: "How is an ACA Sandbox different from a regular Container App?"


      Compare-style query. Skill activates. Explains: ACA Sandbox = single microVM you own + SSH into
      for dev loop, MCP hosting, agent dev. Container App = the existing serverless container service
      (auto-scale 0→N, HTTPS ingress, revisions). Different lifecycle, different audience. References
      public sandboxes-overview.md.


      If the response follows the expected behavior described above, call set_waza_grade_pass.


      If it fails to meet the expected behavior, call set_waza_grade_fail.'
    continue_session: true
