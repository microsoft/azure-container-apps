id: pos-install-aca-linux
name: Install aca CLI on Linux
description: Skill activates. Provides the Linux install one-liner (curl bash), then `aca doctor` + `aca
  login`. Should mention this is the same path used inside other sandboxes/containers for agent-driven
  installs.
tags:
- positive
- install
- linux
- cli
inputs:
  prompt: Install the aca CLI on Ubuntu
expected:
  should_trigger: true
graders:
- type: prompt
  name: behavior-pos-install-aca-linux
  config:
    prompt: 'The user asked: "Install the aca CLI on Ubuntu"


      Skill activates. Provides the Linux install one-liner (curl bash), then `aca doctor` + `aca login`.
      Should mention this is the same path used inside other sandboxes/containers for agent-driven installs.


      If the response follows the expected behavior described above, call set_waza_grade_pass.


      If it fails to meet the expected behavior, call set_waza_grade_fail.'
    continue_session: true
